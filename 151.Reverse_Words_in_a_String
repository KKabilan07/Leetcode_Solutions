class Solution:
    def reverseWords(self, s: str) -> str:
        s = s.strip()
        s_list = s.split()
        n = len(s_list)
        s_list = s_list[::-1]
        return ' '.join(s_list)
        